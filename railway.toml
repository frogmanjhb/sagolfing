[build]
builder = "nixpacks"
buildCommand = "npm install --legacy-peer-deps && chmod -R +x node_modules/.bin && npm run build"

[deploy]
startCommand = "npx serve dist -s -l $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

